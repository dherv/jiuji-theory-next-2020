import React from "react";
import Layout from "../components/layout";
import Note from "../components/note/Note";

const Home = () => (
  <Layout>
    <Note></Note>
  </Layout>
);

export default Home;
